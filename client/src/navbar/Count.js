import React from 'react'

export default function Count(props) {
  return (
      <div className="count">
                  <label className="views">{props.count}</label>
                </div>
  )
}
